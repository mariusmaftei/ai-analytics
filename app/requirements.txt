Flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0
pymongo==4.6.0
google-generativeai==0.8.3
pymupdf==1.23.8
Pillow==10.1.0
pinecone>=3.0.0
requests==2.31.0
gunicorn==21.2.0
psutil==5.9.6
# CPU-only PyTorch (saves ~2-3GB vs GPU version) - must install from PyTorch CPU index
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchvision==0.20.1+cpu
# Pin ultralytics to avoid dependency resolution issues
ultralytics==8.3.233
# Audio processing
# AssemblyAI for Speech-to-Text transcription (free tier: 185 hours/month)
# ElevenLabs for Text-to-Speech (TTS) - optional feature
elevenlabs>=1.0.0
# mutagen for audio metadata extraction (MP3, MP4, OGG, FLAC, etc.)
mutagen>=1.47.0
# librosa and soundfile for audio signal analysis (loudness, peak level, noise level, dynamic range)
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
# requests library (already included) is used for AssemblyAI API calls

